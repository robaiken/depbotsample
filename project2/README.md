# Project 2

A sample Python project that uses uv for dependency management and has a path dependency on project1.

## Features

- Uses uv for dependency management
- Has a path dependency on project1
- Includes a uv.lock file for reproducible builds
